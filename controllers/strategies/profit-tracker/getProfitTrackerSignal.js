async function getProfitTrackerSignal() {
    /*
    {
            signal: "SELL",
            strategy: "profitTracker",
            transactionPerc: 100,
        }
     */

    // empty signal handle with strategiesManager
    return { signal: null };
}

module.exports = getProfitTrackerSignal;
